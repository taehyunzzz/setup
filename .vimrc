" Enable syntax highlighting
syntax on
" Show line numbers (relative numbers are great for jumping lines)
set number
set relativenumber
"
" " Highlight the current line so you don't lose your cursor
set cursorline
"
" " Wrap lines at a character, not in the middle of a word
set linebreak
"
" " Show existing tab with 4 spaces width
set tabstop=4
" " When indenting with '>', use 4 spaces width
set shiftwidth=4
" " On pressing tab, insert 4 spaces
set expandtab

set hlsearch
set scrolloff=8
"
" Auto-indent new lines based on the previous line
set autoindent
"
" Highlight matching brackets (parentheses, curly braces)
set showmatch
"
" Use the system clipboard for copy/paste (requires +clipboard support)
set clipboard=unnamedplus
"
" Enable mouse support (useful for resizing windows or scrolling)
set mouse=a
